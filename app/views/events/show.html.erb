<%= render @event %>

<% if @attendees.include?(current_user) %>
  <span>You are already attending this event!</span>
<% else %>
  <%= button_to "Attend Event", event_events_details_path(@event)%>
<% end %>
<div>
  <%= link_to 'Edit', edit_user_event_path(@event.creator, @event) if @event.creator == current_user %>
</div>
<h3>Attendees</h3>
<ul>
  <% @attendees.each do |attendee|%>
  <li><%= attendee.name %></li>
  <% end %>
</ul>
